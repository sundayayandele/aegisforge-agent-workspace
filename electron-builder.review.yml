# Local review builds must not reuse the signed app's macOS permission identity.
extends: ./electron-builder.yml
appId: ai.aegisforge.agentworkspace.review
productName: AegisForge Agent Workspace Review
extraMetadata:
  name: aegisforge-agent-workspace-review
directories:
  output: release-review
mac:
  identity: null
dmg:
  sign: false
